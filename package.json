{
  "name": "diretoria",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "engines": {
    "node": ">=22.16.0"
  },
  "scripts": {
    "test": "node scripts/run-tests.mjs",
    "check": "npm run test && npm run secrets:check && npm run migrations:check && npm run docs:check",
    "secrets:check": "node scripts/check-secrets.mjs",
    "migrations:check": "node scripts/check-migrations.mjs",
    "docs:check": "node scripts/check-docs.mjs",
    "api": "node --experimental-strip-types apps/api/src/server.ts",
    "admin": "node apps/admin/server.mjs",
    "db:up": "docker compose up -d db",
    "db:down": "docker compose down",
    "db:migrate:docker": "docker compose exec -T db psql -U diretoria -d diretoria -v ON_ERROR_STOP=1 -f /migrations/0001_core_foundation.sql && docker compose exec -T db psql -U diretoria -d diretoria -v ON_ERROR_STOP=1 -f /migrations/0002_seed_rbac.sql"
  }
}
