# RELATÓRIO HML-G0 — DIRETORIA

**Data:** 21/08/2026  
**Gate:** HML-G0 — Fundação  
**Resultado:** **APROVADO**

## 1. Objetivo

Validar a fundação em ambiente persistente e separado de produção antes do Incremento 1.

## 2. GitHub / CI

Repositório: `squalmg/diretoria`

### Hardening

PR #2: `chore: endurecer HML-G0 no Supabase`  
Merge: `7ad961ee394fd3860a9604f3b278387b79612e15`

CI validou em PostgreSQL 18.6 limpo:

- testes de domínio;
- secret scan;
- documentação;
- todas as migrations em ordem;
- 23/23 tabelas da Fundação com RLS;
- PK física de `user_roles`.

### Backup/restore

PR #3: `ci: validar backup e restore do HML-G0`  
Merge: `0650a2ff2024e77c0b984ca294161e79caaeefa4`

Run GitHub Actions: `32543542911`.

O primeiro drill revelou incompatibilidade entre `pg_dump` 16.15 do runner e PostgreSQL 18.6. A abordagem foi alterada: o drill passou a usar as ferramentas PostgreSQL 18 do próprio container.

Resultado final:

- `pg_dump`: PASS;
- criação de banco de restauração: PASS;
- `pg_restore`: PASS;
- dado sintético `RESTORE-PROBE`: preservado;
- 8 roles RBAC: preservadas;
- 23 tabelas com RLS: preservadas;
- banco temporário removido após validação.

## 3. Supabase HML

Projeto: `diretoria-hml`  
Project ref: `heckakjcpwomoucobtau`  
Região: `sa-east-1`  
Estado observado: `ACTIVE_HEALTHY`

Migrations aplicadas:

1. `0001_core_foundation`;
2. `0002_seed_rbac`;
3. `0003_supabase_hml_hardening`.

Validação pós-migration:

- 23/23 tabelas críticas com RLS habilitado;
- 1 PK em `user_roles`;
- índices de cobertura de FKs criados;
- 8 roles;
- 11 permissions.

### Advisors

Antes do hardening, o advisor de segurança apontava `rls_disabled_in_public` como ERROR.

Depois do hardening:

- nenhum ERROR de RLS desabilitado;
- apenas INFO `rls_enabled_no_policy`, intencional porque o HML permanece **default-deny** até políticas explícitas de aplicação serem implementadas;
- performance apresenta apenas INFO de índices ainda não utilizados, esperado em banco recém-criado e sem carga.

## 4. Vercel HML

Projeto: `diretoria-hml`  
Project id: `prj_CSbGzOVsvIkkJLosiemlHmvcG7XV`  
URL: `https://diretoria-hml.vercel.app`

Deployment validado: `dpl_A1T4RoF1PBw1sBpESsgQK7nhZDJD`

Verificações:

- `/`: HTTP 200;
- `/api/health`: HTTP 200;
- ambiente identificado explicitamente como `hml`;
- bundle correspondente versionado em `apps/hml/`;
- nenhum segredo colocado no bundle.

## 5. Observabilidade

- GitHub Actions: disponível e validado;
- logs PostgreSQL Supabase: disponíveis;
- Vercel deployment/runtime: disponível pelo projeto HML;
- advisors Supabase: segurança e performance executados.

## 6. Segurança

- ambiente separado de produção;
- RLS default-deny;
- nenhum segredo versionado;
- CI contém secret scan;
- migrations versionadas;
- nenhuma policy permissiva criada por conveniência.

## 7. Conclusão

HML-G0 atende aos requisitos da Fundação:

- build/check reproduzível;
- deploy persistente;
- PostgreSQL persistente;
- migrations limpas;
- segredos fora do código;
- observabilidade básica;
- backup/restore validado.

### Decisão

**GO para Incremento 1 — Núcleo Econômico.**

Próximo fluxo:

`edição → configuração financeira → custos → receitas garantidas → pagamento HML → crédito → quórum → reembolso → GO/NO-GO`

**Desenvolvido por [Clan Digital](https://clanmarketing.com.br/)**
